export enum HomeSection {
  NEWS = 'news',
  SHOWS = 'shows',
  GAMES = 'games',
  MUSIC = 'music',
  PROJECTS = 'projects',
}

export enum ActivityType {
  GAME = 'GAME',
  SHOW = 'SHOW',
}

export enum StatsTab {
  GAME = 'GAME',
  SHOW = 'SHOW',
}

export enum StatsGameTab {
  GAME_DAYS = 'GAME_DAYS',
}
